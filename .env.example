# Hostname (or IP) of the Raspberry Pi on the local network.
# Used by frontend/vite.config.ts (dev proxy) and deploy.sh (rsync + ssh).
PI_HOST=raspberry-pi-seva.local

# SSH user on the Pi.
PI_USER=seva

# Deploy path on the Pi.
PI_PATH=/drone-scripts/ugv
